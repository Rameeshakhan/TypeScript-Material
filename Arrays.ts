
const superHeros : number[] = [];

const num1 : Array<number> = [] // another syntax 

// Array of Arrays -> 2d Array 
const matrix: number[][] = [
    [255,255,255],
    [0,0,0]
]

superHeros.push(9);
superHeros.push(2);
superHeros.push(4);
superHeros.push(5);

console.log(superHeros);

export {};

num1.push(4)

type obj = {
    name : string
    isActive : boolean
}

const userList: obj[] = []

userList.push({name: "" , isActive : true})
